# Backend API base URL (no trailing slash)
VITE_BACKEND_API_URL=http://localhost:5000/api

# Optional: separate URL for direct axios client (defaults to VITE_BACKEND_API_URL)
# VITE_BACKEND_URL=http://localhost:5000
