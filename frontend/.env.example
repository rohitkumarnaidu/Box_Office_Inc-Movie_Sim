# Backend API base URL (no trailing slash)
# When using Vite proxy (vite.config.js), this can be relative: /api
# Without proxy, set to full URL: http://localhost:5000/api
VITE_BACKEND_API_URL=http://localhost:5000/api
